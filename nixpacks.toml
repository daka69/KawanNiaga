[phases.release]
cmds = []

[start]
cmd = "php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan storage:link && php artisan migrate --force && vendor/bin/heroku-php-apache2 public/"
